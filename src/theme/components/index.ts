import { ComponentStyleConfig } from "@chakra-ui/react";
import { solid, outline, solidSecondary, outlineSecondary } from "./Button";
export const Button: ComponentStyleConfig = {
  variants: {
    solid,
    outline,
    solidSecondary,
    outlineSecondary,
  },
};
